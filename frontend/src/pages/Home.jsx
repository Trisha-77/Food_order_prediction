import Hero from '../components/Hero'; import StatsCards from '../components/StatsCards'; import SectionTitle from '../components/SectionTitle';
const steps = [['bi-person-vcard', 'Enter Customer Details', 'Choose location, time, previous order, cuisine, and order frequency.'], ['bi-search-heart', 'Analyze Preferences', 'The future ML integration will prepare the selected customer attributes.'], ['bi-cpu', 'Apply ML Prediction', 'The trained Random Forest model will be connected through an API later.'], ['bi-bag-check', 'Get Predicted Food', 'View the likely next food choice in one clear result.']];
export default function Home() { return <><Hero /><main><section className="container section-space"><StatsCards /></section><section className="how-section"><div className="container section-space"><SectionTitle centered eyebrow="SIMPLE BY DESIGN" title="How It Works" description="A clear path from customer information to a future model-powered prediction." /><div className="row g-4 mt-1">{steps.map(([icon, title, text], index) => <div className="col-md-6 col-lg-3" key={title}><article className="step-card h-100"><span className="step-number">0{index + 1}</span><i className={`bi ${icon}`} /><h3>{title}</h3><p>{text}</p></article></div>)}</div></div></section></main></>; }
